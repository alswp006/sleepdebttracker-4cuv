// TDD: Placeholder for implementation
// Should return: {unlockedToday: boolean, unlock: () => void}

export function useReward(kind: "report" | "plan") {
  // Implementation pending
  return {
    unlockedToday: false,
    unlock: () => {},
  };
}
